<UserControl x:Class="ICSharpCode.SharpDevelop.Services.SolutionExplorerView"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:services="clr-namespace:ICSharpCode.SharpDevelop.Services">
    <UserControl.Resources>
        <HierarchicalDataTemplate DataType="{x:Type services:SolutionExplorerNodeModel}"
                                  ItemsSource="{Binding Children}">
            <StackPanel Orientation="Horizontal">
                <Image Width="16"
                       Height="16"
                       Margin="0,0,4,0"
                       Source="{Binding Icon}" />
                <TextBlock Text="{Binding Name}" />
            </StackPanel>
        </HierarchicalDataTemplate>
    </UserControl.Resources>
    <Grid>
        <TreeView x:Name="treeView"
                  BorderThickness="0"
                  ItemsSource="{Binding RootNodes}"
                  MouseDoubleClick="TreeViewOnMouseDoubleClick"
                  PreviewMouseRightButtonDown="TreeViewOnPreviewMouseRightButtonDown"
                  SelectedItemChanged="TreeViewOnSelectedItemChanged" />
    </Grid>
</UserControl>
