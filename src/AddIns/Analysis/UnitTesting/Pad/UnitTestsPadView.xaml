<UserControl x:Class="ICSharpCode.UnitTesting.UnitTestsPadView"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ICSharpCode.UnitTesting">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ContentControl x:Name="toolbarHost" Grid.Row="0" />

        <Grid x:Name="treeHost" Grid.Row="1">
            <local:TestTreeView x:Name="treeView" />
            <TextBlock Margin="12"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Top"
                       Foreground="{DynamicResource MutedForeground}"
                       IsHitTestVisible="False"
                       Text="Loading tests…">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>

        <Border Grid.Row="2"
                Padding="8,4"
                Background="{DynamicResource ToolWindowBackground}"
                BorderBrush="{DynamicResource Border}"
                BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Margin="0,0,14,0"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource MutedForeground}"
                           Text="{Binding TotalText}" />
                <StackPanel Margin="0,0,14,0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="8" Height="8" Margin="0,0,5,0" Fill="#1D8049" />
                    <TextBlock Foreground="{DynamicResource MutedForeground}" Text="{Binding PassedText}" />
                </StackPanel>
                <StackPanel Margin="0,0,14,0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="8" Height="8" Margin="0,0,5,0" Fill="#BE3A34" />
                    <TextBlock Foreground="{DynamicResource MutedForeground}" Text="{Binding FailedText}" />
                </StackPanel>
                <StackPanel Margin="0,0,14,0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="8" Height="8" Margin="0,0,5,0" Fill="#916A20" />
                    <TextBlock Foreground="{DynamicResource MutedForeground}" Text="{Binding SkippedText}" />
                </StackPanel>
                <StackPanel Margin="0,0,14,0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="8" Height="8" Margin="0,0,5,0" Fill="#808A94" />
                    <TextBlock Foreground="{DynamicResource MutedForeground}" Text="{Binding NotRunText}" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
